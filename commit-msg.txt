feat: full CRUD + toast for admin products/orders/payments

- Add server actions in admin/(protected)/actions.ts:
  - updateOrderStatusAction, deleteOrderAction
  - toggleProductAction, createProductAction, updateProductAction, deleteProductAction
  - updatePaymentMethodAction, togglePaymentMethodAction, deletePaymentMethodAction
  All emit ?msg=...&toast=ok|err on success/error
- Add Toast client component (slide-in top-right, auto-dismiss 4s,
  success/error variant) — reads ?msg + ?toast from URL
- Replace static admin pages with manager components:
  - ProductsManager: search + grouped table (per game), inline edit,
    add form, toggle switch, delete button, optimistic UI
  - OrdersManager: search + status filter, status select per row,
    inline save (only when dirty), delete button
  - PaymentsManager: search, card layout, inline edit form, toggle
    switch (iOS style), delete button
- All three pages now: gradient hero header + Toast + manager
- Fix: remove non-existent whatsapp_cs column from payment_methods
  select (was returning empty data)
- Add scripts/test-crud.js for E2E verification of all CRUD ops
- Build EXIT=0, all 12 routes compile, runtime verified with
  ersetstore@gmail.com — create/update/toggle/delete all work

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
